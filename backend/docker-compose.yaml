services:
  db:
    image: mariadb:latest
    restart: always
    ports:
      - "3309:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    container_name: bibliotecadb
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data:
